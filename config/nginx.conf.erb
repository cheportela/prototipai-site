worker_processes 1;

events { worker_connections 1024; }

http {
    server {
        listen <%= ENV["PORT"] %>;

        root <%= ENV["ROOT"] || "public" %>;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
